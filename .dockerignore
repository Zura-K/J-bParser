# Context for deploy/nginx/Dockerfile — only the frontend sources and
# the nginx config are needed; keep everything else out of the build.
**
!frontend
!frontend/**
!deploy/nginx
!deploy/nginx/**
frontend/node_modules
frontend/node_modules/**
frontend/dist
frontend/dist/**